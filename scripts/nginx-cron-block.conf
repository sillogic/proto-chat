# ProtoChat nginx 安全配置片段
# 将以下内容加入 /etc/nginx/nginx.conf 的 server {} 块中
# （主项目的 server 块，通常监听 80/443）
#
# 作用：封锁 cron 接口的公网访问，只允许本机调用

    # ---- 封锁 cron 接口（只允许本机） ----
    location /api/cron/ {
        allow 127.0.0.1;
        deny all;
    }
